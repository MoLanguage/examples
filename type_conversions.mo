use nums

fn main() {

    // type conversion through traits ??? But what if conversion can fail? lets figure something out...

    Str thing := "Hallo!"
    Int32 num := 1

    thing.append(num as Str)
}