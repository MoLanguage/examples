fn main() {
  a := get_things()
  
}

// Idea: Have it so that function arguments are always immutable inside the function by default, but you can make them mutable with the mut keyword in the function declaration

fn add_string(string str) str {
  strings:concat(string, "Hello")
  ret string
}
